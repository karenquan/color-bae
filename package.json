{
  "dependencies": {
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "node-fetch": "^2.6.7"
  },
  "scripts": {
    "start": "node server.js",
    "server": "nodemon server.js",
    "heroku-postbuild": "cd client && yarn && yarn run build"
  },
  "name": "color-bae",
  "description": "A React.js app providing colors and color palettes from [colourlovers.com](https://www.colourlovers.com).",
  "version": "1.0.0",
  "main": "server.js",
  "devDependencies": {
    "nodemon": "^2.0.2"
  },
  "engines": {
    "node": "10.16.0"
  }
}
